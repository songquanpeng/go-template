name: Build Releases

on:
  push:
    tags:
      - '*'
jobs:
  release:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: '>=1.19.0'
      - name: Build Binaries
        run: |
          go mod download
          go build -ldflags "-s -w -X 'go-template/common.Version=$(git describe --tags)'" -o go-template.exe
          GOOS=linux GOARCH=amd64 go build -o go-template-linux-amd64
          GOOS=linux GOARCH=arm GOARM=7 go build -o go-template-linux-arm7
          GOOS=linux GOARCH=arm64 go build -o go-template-linux-arm64
          GOOS=windows GOARCH=amd64 go build -o go-template-windows-amd64.exe
          GOOS=darwin GOARCH=amd64 go build -o go-template-darwin-amd64
          GOOS=darwin GOARCH=arm64 go build -o go-template-darwin-arm64
      - name: Upload Binaries
          uses: softprops/action-gh-release@v0.1.13
          if: startsWith(github.ref, 'refs/tags/')
          with:
            files: ${{runner.workspace}}/go-template*
          env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}